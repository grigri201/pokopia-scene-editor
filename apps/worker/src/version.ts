import { assetCatalog, pokemonThemeCatalog } from '@pokopia-scene-editor/scene-core';

export const serviceVersion = '0.1.0';
export const schemaVersion = 1;
export const catalogVersion = `assets:${assetCatalog.length};pokemon:${pokemonThemeCatalog.length}`;
